# Próxima ação

**Fase 1 · S2** — instalar Nativewind. Conferir na doc oficial se a v4 suporta
RN 0.86 **antes** de instalar; depois `babel.config.js`, `metro.config.js`,
`global.css`, `nativewind-env.d.ts`, e `import "../global.css"` no `_layout.tsx`.
Rodar com `npx expo start --clear`. Pronto quando `<Text className="text-red-500">`
sair vermelho.

> ⚠️ Fase 0 ainda pendente: blocos no calendário, testemunha, critério de parada.

---

<!--
Última coisa de toda sessão: substituir o texto acima pela próxima ação concreta.
Com nome de arquivo. Assim:

  ✅ "terminar o onPressOut do Button em src/components/Button.tsx — falta o
      withTiming da subida"
  ❌ "mexer no design system"

Se você parou travado num erro, escreva o erro aqui também.
-->

## Critério de parada

<!-- Escreva agora, com a cabeça fria. Um número e uma data.
     Ex.: "se na semana 20 eu tiver menos de 30 usuários ativos, eu paro e reavalio" -->

_(a preencher na Fase 0)_

## Blocos da semana

<!-- Os dias e horários que você marcou no calendário. -->

_(a preencher na Fase 0)_
